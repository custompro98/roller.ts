import type { Modifier } from './types';
export default class StraightValue implements Modifier {
    private mod;
    constructor(mod: number);
    value(): number;
}
//# sourceMappingURL=straight-value.d.ts.map